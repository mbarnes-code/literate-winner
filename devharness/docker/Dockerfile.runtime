# Dockerfile.runtime — bundled runtime for the `docker` sandbox backend.
# Spec: docs/devharness-spec.md §4.6, §7.3

FROM python:3.12-slim
# TODO: install git, gh, jq, ripgrep, fd, ast-grep, and language toolchains.
