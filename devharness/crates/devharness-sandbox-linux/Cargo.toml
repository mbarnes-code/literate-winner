[package]
name = "devharness-sandbox-linux"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"

# Placeholder — see docs/devharness-spec.md §4.6 and §7.4.
# Wraps Landlock + bwrap for Linux-native sandbox.

[lib]
crate-type = ["cdylib"]

[dependencies]
# TODO: landlock, nix, pyo3
