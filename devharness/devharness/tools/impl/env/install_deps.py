"""install_deps — install dependencies from lockfile.

Placeholder — implementation deferred.

- Category: Env & Deps
- Sandbox class: ME
- Approval class: S
- Parallel-safe: ❌
- Default timeout: 600s
- Backing CLI: uv sync --frozen / pnpm install --frozen-lockfile / cargo build --locked / go mod download

Spec: docs/devharness-spec.md §5.6
"""

# TODO: implement via @register decorator from devharness.tools.registry
