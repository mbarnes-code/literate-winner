"""run_single_test — run a single named test.

Placeholder — implementation deferred.

- Category: Testing
- Sandbox class: MF
- Approval class: S
- Parallel-safe: ❌
- Default timeout: 300s
- Backing CLI: pytest {path}::{name} / vitest run -t / cargo test --exact / go test -run

Spec: docs/devharness-spec.md §5.2
"""

# TODO: implement via @register decorator from devharness.tools.registry
